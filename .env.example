# PostgreSQL Database Configuration (optional - falls back to simulated engine)
DB_HOST="localhost"
DB_PORT="5432"
DB_USER="postgres"
DB_PASSWORD="your_password_here"
DB_NAME="pentaguard"
DATABASE_URL=""

# JWT Secret - REQUIRED in production. Generate with: openssl rand -base64 32
JWT_SECRET="change-this-to-a-random-secret"
